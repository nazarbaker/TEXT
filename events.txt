Events - Події

Як підписатися на події?

На подію можна підписатися одним із таких способів:

1. Підписатися через код HTML (не рекомендується використовувати).
	d:\Programuvannya\course\JS\js-exercises\event\events-in-html.html

2. Через код JavaScript за допомогою присвоєння елементу властивості on [подія], де [подія] - це ім'я певної події.
---------------------------

3.Через код JavaScript за допомогою спеціального методу addEventListener. (Рекомендується)

element.addEventListener(event, callback, phase);
element.removeEventListener(event, callback, phase); // видаляє прослушку.

event (обов'язковий) - ім'я події, на яку потрібно підписатися (прослухати);

callback (обов'язковий) - функція (анонимна чи іменована), котра буде виконувати обрабку події;

phase (не обов'язковий) - этап, на котрому буде перехватуватися подія.
Даний параметр приймає 2 значення: true (на этапі занурення (перехвату) події) і false
(на этапі вспливання події). Якщо даний параметр не вказати, то буде використовуватися false. // не поняв!


Основні події в javaScript
onclick — нажатие кнопки мышки.
ondblclick — двойное нажатие кнопки мышки.
onmousedown — кнопка мышки нажата (опущена вниз).
onmousemove — мышка двигается по объекту (возникает множество событий, движение мышки по 1px = 1 событие).
onmouseout — мышка уходит с объекта.
onmouseover — мышка наезжает на нужный объект.
onmouseup — кнопка мышки отпущена (поднята вверх).
onkeydown — кнопка клавиатуры нажата (опущена вниз).
onkeypress — нажатие кнопки клавиатуры.
onkeyup — кнопка клавиатуры отпущена (поднята вверх).
onblur — потеря фокуса ввода (мигающий курсор).
onfocus — момент получения фокуса ввода данным элементом.
onchange — генерируется элементом управления, после потери фокуса, если изменился текст (например, в текстовом поле). Исключение — элемент списка ‘select’, в нем это событие генерируется сразу, а не после потери фокуса.
onload — генерируется, когда в окне закончилась загрузка страницы. Обычно используют, когда надо запустить какой-то javaScript код после полной загрузки страницы.
onunload — возникает перед тем, как документ будет выгружен из окна (т.е. когда закрываем этот документ или переходим на другой документ).
onreset — форма сброшена.
onsubmit — форма данных отправлена.

--------------------------------
 http://javascript.ru/tutorial/dom/search
 https://learn.javascript.ru/searching-elements-dom#document-getelementbyid-или-просто-id до XPath
 http://itchief.ru/lessons/javascript/javascript-ways-to-subscribe-to-events
 http://dargame.ru/prog/sobytija-javascript?page/sobytija-javascript -  почитати пр події
 Коли все зрозумієш прочитати і зробити
 https://learn.javascript.ru/custom-events

 http://www.tigir.com/javascript_select.htm
